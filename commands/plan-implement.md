---
name: plan-implement
description: Stage 6 of /workflow — execute approved tasks.md with tests, per-task reviewer PASS, and checkpoint gates.
argument-hint: "<feature folder or tasks.md path, optionally a task ID to resume from>"
---

Invoke the `plan-implement` skill for: $ARGUMENTS
