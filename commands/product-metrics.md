---
name: product-metrics
description: Stages P6 and P7 of /product-workflow — set the main number, the weekly numbers, the early signal, and the things that must not become worse. Then record the real numbers after launch.
argument-hint: "<the PRD or the change in user behavior, or the initiative folder>"
---

Invoke the `product-metrics` skill for: $ARGUMENTS
