---
name: workflow
description: Run the full feature delivery pipeline — Planning → Implement → Review → QA — with approval gates; resumes from existing artifacts.
argument-hint: "<feature idea, or existing feature folder to resume>"
---

Invoke the `workflow` skill for: $ARGUMENTS
