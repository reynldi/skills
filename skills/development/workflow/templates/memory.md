# {Feature Name} — Working Memory

<!-- Durable, append-only shared memory for every agent (local or delegated) working this feature. Append; never delete — strike through superseded entries (~~like this~~) with a pointer to the replacement. Keep entries one line each; link to files instead of quoting them. -->

## Project facts

<!-- Stage 0 discovery: stack, commands, conventions, folder layout, available reviewer agents. -->

- Build `{cmd}` · test `{cmd}` · run `{cmd}`
- {convention or fact}

## Decisions

| #   | Decision   | Why    | Stage/agent | Date   |
| --- | ---------- | ------ | ----------- | ------ |
| 1   | {decision} | {why}  | {stage}     | {date} |

## Tried & failed (do not repeat)

- {approach → outcome}

## Open questions

- {question} → owner: {user/stage}

## Delegation log

| Handoff                    | Provider/model | Outcome                 |
| -------------------------- | -------------- | ----------------------- |
| handoff/{NNN-stage}.md     | {provider}     | {PASS/FAIL — one line}  |
